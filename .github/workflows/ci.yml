name: ci

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        ATOM_CHANNEL: ['stable','beta','dev']
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
